{{#each users as |user| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{user.post_content}}</h2>
    {{!-- {{#each posts as |post|}}
    <h2>{{post.description}}</h2>
    {{/each}} --}}
  </div>
  <div class="col-md-6">
    <h3><a href="mailto:{{user.email}}">{{user.email}}</a></h3>
  </div>
</div>
{{/each}}
